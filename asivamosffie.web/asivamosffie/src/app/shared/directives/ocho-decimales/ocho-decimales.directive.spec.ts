import { OchoDecimalesDirective } from './ocho-decimales.directive';

describe('OchoDecimalesDirective', () => {
  it('should create an instance', () => {
    const directive = new OchoDecimalesDirective();
    expect(directive).toBeTruthy();
  });
});
